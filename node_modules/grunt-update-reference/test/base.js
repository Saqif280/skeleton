/**
 * @name 平台签到功能
 * @author x
 * @time 2015-3-24
 * @Depends
 *   base.css
 * @example
 * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({ * new SC.Sign({
 * }?t=1459326558061);
 */

+function () {
    //reference css file;
    var css  = "./base.css?h=636a9a6e86&t=1459343221626",
        xcss = ('./xcss/base.css?h=636a9a6e86&t=1459343221626');
    foo.base.cssText = "top:1px;float:left;opacity:.5";
    elem.base.cssText.() || undefined;
    
}();

if ( !jQuery.support.style ) {
    jQuery.attrHooks.style = {
        get: function ( elem ) {
            // Return undefined in the case of empty string
            // Normalize to lowercase since IE uppercases css property names
            return elem.base.cssText.toLowerCase() || undefined;
        },
        set: function ( elem, value ) {
            return ( elem.base.cssText = value + "" );
        }
    };
}
